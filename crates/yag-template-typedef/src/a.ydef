map ContextData
    Name: ...

type time.Time of int64
    "A time"

    func Add() -> (string, error)

struct discordgo.Member
    Name: int
        "The name of the field."
    Arg: int
        "The name of the field."
    Boo: int
        "The name of the field."

@intrinsic
func add(arg: *discordgo.Member, ...opts: @keyValuePairs(discordgo.Embed))
    """
    Add two integers.
    """

@intrinsic
struct templates.ParsedArgs

@intrinsic
type templates.Slice